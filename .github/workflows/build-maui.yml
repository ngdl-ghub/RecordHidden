name: Build MAUI iOS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x   # cài SDK .NET 8 ổn định

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 16.4     # runner có iOS SDK 16.x

      - name: Install MAUI iOS workload
        run: dotnet workload install maui-ios

      - name: Clean project
        run: dotnet clean RecordHidden.csproj

      - name: Restore NuGet packages
        run: dotnet restore RecordHidden.csproj

      - name: Build MAUI iOS app
        run: dotnet build RecordHidden.csproj -c Release -f net8.0-ios16.0 -v diag -p:TreatWarningsAsErrors=false

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: "**/bin/Release/net8.0-ios16.0/**/*.app"